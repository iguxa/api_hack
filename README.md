# K20 - система коллективных принятий решений

http://api.tsybykov.tk/

## Install

1. Vue CLI should be installed globally:
   ```
   npm install @vue/cli -g
   ```
1. Install dependencies:
   ```
   cd front
   npm i
   ```
